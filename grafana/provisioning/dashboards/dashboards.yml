apiVersion: 1
providers:
  - name: "Arche (read-only)"
    orgId: 1
    folder: "Arche"
    type: file
    disableDeletion: true
    editable: false
    allowUiUpdates: false
    options:
      path: /etc/grafana/provisioning/dashboards/json
